{
  "version": "1.0.10",
  "author": "Vladimir Chirikov",
  "scripts": {
    "install_tools": "npm install -g commitizen cz-conventional-changelog",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md --same-file --release-count 1"
  }
}
